export interface ButtonProps  {
  text: string; // A interrogação indica que a propriedade é opcional
  /*color: string; */
}
export interface CardsProps  {
  texth1: string; // A interrogação indica que a propriedade é opcional
  textp: string;
  texta: string;
  img: string;
}